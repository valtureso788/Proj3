1. players.splice(i);
	когда нажимаешь крестик удаляется не только этот участник а все кто стоит после него, потому что в коде не указано сколько именно 	удалять

2. timerInterval = setInterval(() => {
  	seconds--;
  	updateTimerDisplay();
	}, 1000);
	нет проверки на таймер, после 0 идет в минус

3. счетчик учасников не обновляется
function renderPlayers() {
  playersList.innerHTML = "";

  players.forEach((name, i) => {
    // ... создание элементов
  });
 тут 
}

4. если нажать сгенерировать список учасников перемешивается, если тыкать то мешаться будет из перемешанного что не логично